name: GitHub Actions Demo
on: [pull_request]
env:
  node-version: '14.x'

jobs:
  dummy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Run a one-line script
        run: echo Hello, world!

  Build-And-Package:
    uses: adelanre/shared-pipeline/.github/workflows/ci/node.js.yml@main
    with:
      nodeVersion: "10.1"
  
  # Docker-Build-Push:
  #   uses: adelanre/shared-pipeline/ci/docker.yml@main
  #   with:
  #     imageTag: ${{ GITHUB_SHA }} #change to git tag or commit
  # deploy-app:
  #   uses: https://github.com/adelanre/shared-pipeline/blob/main/cd/eks.yml@main

# name: CD
# on: [workflow_call]

# jobs:
#   deploy_to_review:
#      ...

#   deploy_to_staging:
#      ...

#   deploy_to_production:
#     environment: production
#     …
