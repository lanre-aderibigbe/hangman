name: GitHub Actions Demo
on: [pull_request,workflow_dispatch]

jobs:
  Build-And-Package:
    uses: adelanre/shared-pipeline/.github/workflows/ci/node.js.yml@main
    with:
      nodeVersion: "10.1"
  # Docker-Build-Push:
  #   uses: adelanre/shared-pipeline/.github/workflows/ci/docker.yml@main
  #   with:
  #     imageTag: ${GITHUB_RUNNER_TAG} #change to git tag or commit
  # cd:
  #   uses: https://github.com/adelanre/shared-pipeline/blob/main/cd/eks.yml@main